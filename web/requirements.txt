# The web layer's own environment; never installed into the runtime image,
# whose content is part of every run fingerprint.
fastapi>=0.115
uvicorn>=0.30
jinja2>=3.1
numpy>=1.26
plotly>=5.20      # only for the plotly.min.js it ships, served to the browser
pyyaml>=6
httpx>=0.27       # tests
pytest>=8         # tests
